spring:
  profiles:
    active: prod

server:
  tomcat:
    uri-encoding: UTF-8
  location: 127.0.0.1
  servlet:
    session:
      timeout: 600
    context-path: /
# logging config
logging:
  config: classpath:logback.xml
